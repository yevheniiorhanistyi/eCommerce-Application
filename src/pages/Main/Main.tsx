import { Container, Box } from '@mui/material';

import { PromoSlider, BestSellers, PreviewBanner } from '../../components';

const Main: React.FC = () => (
  <>
    {/* <PromoSlider /> */}
    <PreviewBanner />
    <BestSellers />
  </>
);
export default Main;
